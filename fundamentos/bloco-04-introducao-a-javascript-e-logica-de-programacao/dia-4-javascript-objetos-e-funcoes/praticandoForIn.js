let cars = ['Saab', 'Volvo', 'BMW'];

for (let index in cars) {
  console.log(cars[index]);
}

/**let car = {
    type: 'Fiat',
    model: '500',
    color: 'white',
  };
  
  for (let banana in car) {
    console.log(banana, car[banana]);
  }
**/
